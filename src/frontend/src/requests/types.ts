export interface Category { id: string; name: string; level: 'MAJOR' | 'MIDDLE' | 'SERVICE'; externalCode: string | null; sortOrder: number }
export interface RequestField { id: string; fieldKey: string; label: string; inputType: string; required: boolean; placeholder: string | null; options: string[]; helperText: string | null; validationRule: string; sortOrder: number }
export interface AdministrativeArea { id: string; name: string; areaCode: string; parentId: string | null; parentName: string | null }
export interface RequestFile { id: string; requestFieldId: string | null; purposeCode: string; fileName: string; contentType: string; sizeBytes: number; scanStatus: string; malwareScanStatus: string; privacyInspectionStatus: string; sanitizationStatus: string; providerVisibilityStatus: string; displayOrder: number; downloadUrl: string }
export interface ServiceRequestListItem { id: string; title: string; status: string; displayStatus: string; domain: 'GENERAL' | 'INTERIOR' | 'EMERGENCY'; categoryPath: string; createdAt: string; desiredAt: string | null; quoteCount: number; siteVisitProposalCount: number; transactionId: string | null }
export interface ServiceRequestAnswer { fieldId: string; fieldKey: string; label: string; inputType: string; value: unknown }
export interface ServiceRequestDetail extends ServiceRequestListItem { description: string | null; isUrgent: boolean; majorCategoryId: string; middleCategoryId: string; serviceCategoryId: string; administrativeAreaId: string | null; administrativeAreaName: string | null; detailAddress: string | null; detailAddressDisclosureCode: 'AFTER_SELECTION' | 'BEFORE_QUOTE'; selectedProviderId: string | null; selectedProviderName: string | null; canEdit: boolean; canCancel: boolean; canPublish: boolean; answers: ServiceRequestAnswer[]; files: RequestFile[] }
export interface ApiErrorBody { businessCode: string; message: string; fieldErrors?: Record<string, string[]>; traceId?: string }
