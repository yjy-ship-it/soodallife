import { useCallback, useEffect, useState } from 'react'
import { navigate } from '../auth/routing'
import { ProviderAppLayout } from './ProviderAppLayout'
import { cancelProviderExit, createProviderExit, getProviderExit, type ProviderExitDashboard } from './providerExitApi'
import './providerExit.css'

const money=(value:number)=>`${value.toLocaleString('ko-KR')}원`
const label=(value:string)=>({PROVIDER_ROLE_EXIT:'공급자 활동만 종료',ACCOUNT_WITHDRAWAL:'전체 계정 탈퇴 요청',REQUESTED:'신청됨',UNDER_REVIEW:'관리자 검토 중',REFUND_REQUIRED:'환불 필요',BLOCKED_BY_ACTIVE_WORK:'진행 업무 확인 필요',READY_TO_COMPLETE:'종료 준비 완료',COMPLETED:'종료 완료',REJECTED:'거절',CANCELLED:'취소',APPROVED:'승인',PENDING:'검토 대기'}[value]??value)

export function ProviderExitPage(){const [data,setData]=useState<ProviderExitDashboard|null>(null);const [type,setType]=useState('PROVIDER_ROLE_EXIT');const [reason,setReason]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const load=useCallback(()=>getProviderExit().then(setData).catch(e=>setError(e instanceof Error?e.message:'종료 준비상태를 불러오지 못했습니다.')),[]);useEffect(()=>{void load()},[load]);const submit=async()=>{if(!reason.trim())return;setBusy(true);setError('');try{await createProviderExit(type,reason);setReason('');await load()}catch(e){setError(e instanceof Error?e.message:'신청하지 못했습니다.')}finally{setBusy(false)}};const cancel=async()=>{if(!data?.request)return;const value=prompt('신청 취소 사유를 입력해 주세요.');if(!value)return;setBusy(true);try{await cancelProviderExit(data.request.id,value,data.request.rowVersion);await load()}catch(e){setError(e instanceof Error?e.message:'취소하지 못했습니다.')}finally{setBusy(false)}};return <ProviderAppLayout><section className="exitHero"><div><p>MY SOODAL</p><h1>공급자 활동 종료·탈퇴</h1><span>신청 즉시 계정이나 기록을 삭제하지 않습니다. 진행 업무와 충전금 환불을 확인한 후 관리자가 완료합니다.</span></div><button onClick={()=>navigate('/provider/onboarding')}>공급자 설정으로</button></section>{error&&<p className="exitError">{error}</p>}{!data?<p className="exitEmpty">준비상태를 확인하고 있습니다.</p>:<><section className="exitStatusGrid"><article><span>공급자 상태</span><strong>{data.activityStatus}</strong><small>{data.approvalStatus}</small></article><article><span>진행 업무</span><strong>{data.readiness.activeWorkCount}건</strong><small>분쟁 {data.readiness.openDisputeCount}건</small></article><article><span>가용 충전금</span><strong>{money(data.readiness.wallet.availableBalance)}</strong><small>예약금 {money(data.readiness.wallet.reservedBalance)}</small></article><article><span>현재 판정</span><strong>{label(data.readiness.recommendedStatus)}</strong><small>{data.readiness.canComplete?'관리자 완료 가능':'선행 처리 필요'}</small></article></section><section className="exitPanel"><h2>종료 전 확인사항</h2><p>{data.readiness.guidance}</p>{data.readiness.blockers.length?<div className="exitBlockers">{data.readiness.blockers.map(v=><span key={v.code}>{v.label} <b>{v.count}건</b></span>)}</div>:<p className="exitReady">진행 중 업무 차단항목이 없습니다.</p>}<ul><li>충전금 잔액이 있으면 원장 기반 환불과 지급 확인 후 종료됩니다.</li><li>과거 Wallet 원장·거래·A/S·분쟁·감사 이력은 삭제하지 않습니다.</li><li>보존기간과 최종 파기정책은 현재 POLICY_REQUIRED이며 임의 기간을 적용하지 않습니다.</li><li>실제 PG 환불·은행 송금은 아직 연동되지 않았습니다.</li></ul></section>{data.request?<section className="exitPanel"><h2>내 신청 상태</h2><dl><div><dt>신청 유형</dt><dd>{label(data.request.requestType)}</dd></div><div><dt>업무 상태</dt><dd>{label(data.request.status)}</dd></div><div><dt>관리자 검토</dt><dd>{label(data.request.reviewStatus)}</dd></div><div><dt>환불</dt><dd>{data.request.refundStatus?label(data.request.refundStatus):'요청 없음'}</dd></div></dl>{data.request.decisionReason&&<p>관리자 사유: {data.request.decisionReason}</p>}{!['COMPLETED','REJECTED','CANCELLED'].includes(data.request.status)&&<button className="exitSecondary" disabled={busy} onClick={()=>void cancel()}>신청 취소</button>}</section>:<section className="exitPanel"><h2>공급자 활동 종료 신청</h2><label>신청 범위<select value={type} onChange={e=>setType(e.target.value)}><option value="PROVIDER_ROLE_EXIT">공급자 활동만 종료</option><option value="ACCOUNT_WITHDRAWAL">전체 계정 탈퇴 요청</option></select></label>{type==='PROVIDER_ROLE_EXIT'&&data.hasCustomerRole&&<p className="exitInfo">고객 역할은 유지됩니다.</p>}{type==='ACCOUNT_WITHDRAWAL'&&<p className="exitWarning">전체 계정 삭제·개인정보 파기는 정책 미확정으로 자동 처리되지 않습니다. 공급자 역할 종료 이후 별도 검토됩니다.</p>}<label>신청 사유<textarea maxLength={1000} value={reason} onChange={e=>setReason(e.target.value)} placeholder="활동 종료 사유를 입력해 주세요." /></label><button className="exitPrimary" disabled={busy||!reason.trim()} onClick={()=>void submit()}>관리자 검토 요청</button></section>}</>}</ProviderAppLayout>}
