export type SubscriptionService = { id: string; majorName: string; middleName: string; serviceName: string; categoryPath: string }
export type CareProduct = { id: string; serviceCategoryId: string; serviceName: string; productName: string; description: string | null; serviceScope: string; visitsPerPeriod: number; expectedDurationMinutes: number; billingPeriodCode: string; monthlyAmount: number | null; visitAmount: number | null; effectiveFrom: string; effectiveTo: string | null }
export type Recurrence = { frequencyTypeCode: string; intervalValue: number; visitsPerPeriod: number | null; weekdays: number[]; preferredTimeFrom: string; preferredTimeTo: string | null; expectedDurationMinutes: number; startDate: string; endDate: string | null }
export type CareRequest = { id: string; requestNumber: string; serviceCategoryId: string; serviceName: string; careProductId: string | null; careProductName: string | null; requestTypeCode: string; requestedScope: string; preferredStartDate: string; administrativeAreaId: string; areaName: string; customerAddress: string | null; statusCode: string; applicationCount: number; providerSelected: boolean; recurrence: Recurrence; createdAt: string; rowVersion: string }
export type RatingAverage = { itemId: string; itemCode: string; itemName: string; averageValue: number; ratingCount: number; minValue: number; maxValue: number }
export type CareApplication = { id: string; providerId: string; providerName: string; proposedScope: string; proposedMonthlyAmount: number | null; proposedVisitAmount: number | null; availableSchedule: string | null; trustScore: number | null; trustGrade: string | null; trustEvaluationStatus: string; trustDisplay: string; publicReviewCount: number; ratingItemAverages: RatingAverage[]; providerApprovalSummary: string; serviceApprovalSummary: string; requirementSummary: string; statusCode: string; submittedAt: string; isSelected: boolean; rowVersion: string }
export type CareContract = { id: string; contractNumber: string; requestId: string; serviceName: string; careProductName: string | null; providerId: string; providerName: string; statusCode: string; statusDisplay: string; startedAt: string; endedAt: string | null; pauseStartedAt: string | null; resumePlannedAt: string | null; terminationRequestedAt: string | null; terminatedAt: string | null; monthlyAmount: number | null; visitAmount: number | null; currencyCode: string; nextVisitAt: string | null; priceSnapshotJson: string; serviceScopeSnapshotJson: string; recurrenceSnapshotJson: string; providerTrustScoreSnapshot: number | null; providerReplacementRequiresSupport: boolean; rowVersion: string }
export type CareVisit = { id: string; contractId: string; contractNumber: string; serviceName: string; visitNo: number; providerId: string; providerName: string; scheduledStartAt: string; scheduledEndAt: string | null; statusCode: string; statusDisplay: string; visitVerified: boolean; providerCompletionSubmitted: boolean; customerConfirmed: boolean }
export type ScheduleChange = { id: string; visitId: string; oldStartAt: string; oldEndAt: string | null; newStartAt: string; newEndAt: string | null; reason: string; statusCode: string; requestedAt: string; decidedAt: string | null; rowVersion: string }
export type VisitFile = { id: string; fileName: string; contentType: string; sizeBytes: number; displayOrder: number; downloadUrl: string }
export type CareVisitDetail = { visit: CareVisit; visitVerificationResult: string | null; workCompletedAt: string | null; completionChecklistJson: string | null; completionNote: string | null; customerProgressDisplay: string; files: VisitFile[]; reviewId: string | null; afterServiceId: string | null; disputeId: string | null; scheduleChanges: ScheduleChange[] }
export type CareHome = { eligibleServiceCount: number; activeProductCount: number; openRequestCount: number; activeContractCount: number; upcomingVisitCount: number; recentCompletedVisitCount: number; unreadNotificationCount: number; upcomingVisits: CareVisit[]; recentCompletedVisits: CareVisit[] }
export type PaymentMethod = { id: string; paymentMethodTypeCode: string; providerCode: string | null; maskedDisplayText: string | null; statusCode: string; isDefault: boolean; registeredAt: string }
export type Refund = { id: string; typeCode: string; requestedAmount: number; approvedAmount: number | null; statusCode: string; requestedAt: string; completedAt: string | null }
export type PaymentHistory = { id: string; contractId: string; contractNumber: string; serviceName: string; billingPeriodStart: string; billingPeriodEnd: string; requestedAmount: number; currencyCode: string; statusCode: string; requestedAt: string; processedAt: string | null; failureReason: string | null; refunds: Refund[] }
